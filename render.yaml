services:
  - type: web
    name: survey-pdf-backend
    runtime: python
    rootDir: satisfaction-app/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
